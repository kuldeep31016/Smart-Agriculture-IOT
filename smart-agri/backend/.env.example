# AgriSense Backend Environment Variables
PORT=5000

# ── Firebase Configuration ──────────────────────────────────────────────────
# Option 1 (recommended): Paste entire serviceAccountKey.json content as string
# FIREBASE_CONFIG={"type":"service_account","project_id":"your-project-id",...}

# Option 2: Place serviceAccountKey.json in the backend/ folder
# Download from Firebase Console → Project Settings → Service Accounts → Generate New Private Key
